import CommentModel from "../model/comment.model";

export const createComment = async (postId, comment, authorId) => {
  try {
    const comment = await CommentModel(data);
  } catch (error) {
    throw new Error(error);
  }
};

export const updateComment = async () => {
  try {
  } catch (error) {
    throw new Error(error);
  }
};

export const deleteComment = async () => {
  try {
  } catch (error) {
    throw new Error(error);
  }
};
